let fixedType: number | string;

// fixedType = 10;
// fixedType = "Hello";

// ===================================================

let ableType: "enable" | "disable";

// ableType = "enable";
// ableType = "disable";
